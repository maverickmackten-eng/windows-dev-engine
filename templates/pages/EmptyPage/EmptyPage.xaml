<UserControl x:Class="__APP_NAME__.Views.Pages.EmptyPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundDeepBrush}">
    <!--
        EmptyPage.xaml — Blank page scaffold with correct bindings
        USAGE: Copy this for each new page. Replace __PAGE_NAME__ and __APP_NAME__.
        DataContext: __APP_NAME__.ViewModels.__PAGE_NAME__ViewModel
    -->
    <Grid>
        <Border Background="#CC0A0D12" Panel.ZIndex="99"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="Loading..." HorizontalAlignment="Center" VerticalAlignment="Center"
                       FontSize="16" Foreground="{DynamicResource TextSecondaryBrush}"/>
        </Border>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding PageTitle, FallbackValue='Page Title'}"
                       FontFamily="Segoe UI Black" FontSize="32"
                       Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding StatusMessage, FallbackValue='Build your content here.'}"
                       FontSize="14" Foreground="{DynamicResource TextSecondaryBrush}"
                       HorizontalAlignment="Center" Margin="0,12,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
